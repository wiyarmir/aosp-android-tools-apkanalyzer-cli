Docker apkanalyzer-cli
----------------------

Run the latest, patched version of `apkanalyzer-cli`.

Usage
=====

```bash
docker run -v ~/:/root "wiyarmir/aosp-android-tools:apkanalyzer-cli" files list --raw-size /root/myapk.apk
# or any other command
```
